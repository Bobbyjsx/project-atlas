apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: project-atlas
  annotations:
    run.googleapis.com/launch-stage: BETA
spec:
  template:
    metadata:
      annotations:
        # Crucial: enable multiple containers
        run.googleapis.com/container-dependencies: '{"atlas-gateway": ["keysentry-sidecar"]}'
        # Direct VPC Egress to reach internal VM IPs (like the database at 10.218.0.2)
        run.googleapis.com/vpc-access-egress: "private-ranges-only"
        run.googleapis.com/network-interfaces: '[{"network":"default","subnetwork":"default"}]'
    spec:
      containers:
        # --- The Ingress Gateway Container ---
        - image: gcr.io/${GCP_PROJECT_ID}/project-atlas:${ATLAS_VERSION}
          name: atlas-gateway
          ports:
            - containerPort: 8080
          env:
            - name: API_KEY_KEYSENTRY
              valueFrom:
                secretKeyRef:
                  name: api-key-keysentry  # Name of the secret in GCP Secret Manager
                  key: latest
            - name: API_KEY_CHESS
              valueFrom:
                secretKeyRef:
                  name: api-key-chess
                  key: latest
          resources:
            limits:
              cpu: "1000m"
              memory: "512Mi"

        # --- Sidecar 1: KeySentry API ---
        - image: gcr.io/${GCP_PROJECT_ID}/keysentry-api:${KEYSENTRY_VERSION}
          name: keysentry-sidecar
          env:
            - name: PORT
              value: "8001"
            # It fetches the DB connection string natively from Google Secret Manager
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: keysentry-db-url           # Name of the secret in GCP Secret Manager
                  key: latest                      # Use the latest version of the secret
            - name: GOTRUE_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: atlas-gotrue-jwt-secret
                  key: latest
            - name: GOTRUE_URL
              valueFrom:
                secretKeyRef:
                  name: atlas-gotrue-url
                  key: latest
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: keysentry-encryption-key
                  key: latest
            - name: TRIGGER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: keysentry-trigger-api-key
                  key: latest
            - name: INTERNAL_API_SECRET
              valueFrom:
                secretKeyRef:
                  name: keysentry-internal-api-secret
                  key: latest
          startupProbe:
            tcpSocket:
              port: 8001
            initialDelaySeconds: 2
            timeoutSeconds: 2
            periodSeconds: 5
            failureThreshold: 5
